name: Local Toolchain Export 2026-09-10

on:
  push:
    branches:
      - maintenance/local-mirror-export-2026-09-10

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout active development branch
        uses: actions/checkout@v4
        with:
          ref: development-first-working-liliya-cold-start-fixture-v0.1
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 9.6.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.6.1'
          cache-disabled: true

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install local compile SDK
        shell: bash
        run: |
          set -euo pipefail
          sdkmanager \
            "platforms;android-35" \
            "build-tools;35.0.0" \
            "platform-tools"

      - name: Warm project-only Gradle home
        shell: bash
        run: |
          set -euo pipefail
          LILIYA_GRADLE_HOME="$RUNNER_TEMP/liliya-gradle-home"
          rm -rf "$LILIYA_GRADLE_HOME"
          mkdir -p "$LILIYA_GRADLE_HOME"
          GRADLE_USER_HOME="$LILIYA_GRADLE_HOME" gradle \
            :android-app:testDebugUnitTest \
            :android-runtime:testDebugUnitTest \
            :core:test \
            :android-app:compileDebugAndroidTestKotlin \
            --console=plain
          GRADLE_USER_HOME="$LILIYA_GRADLE_HOME" gradle \
            :core:test \
            --offline --console=plain

      - name: Pack split local toolchain
        shell: bash
        run: |
          set -euo pipefail
          GRADLE_BIN="$(readlink -f "$(command -v gradle)")"
          GRADLE_HOME="$(cd "$(dirname "$GRADLE_BIN")/.." && pwd)"
          mkdir -p "$RUNNER_TEMP/local-toolchain"
          JAVA_REAL="$(readlink -f "$JAVA_HOME")"
          tar --dereference -C "$(dirname "$JAVA_REAL")" -czf "$RUNNER_TEMP/local-toolchain/temurin-jdk17.tar.gz" "$(basename "$JAVA_REAL")"
          tar -C "$(dirname "$GRADLE_HOME")" -czf "$RUNNER_TEMP/local-toolchain/gradle-9.6.1-runtime.tar.gz" "$(basename "$GRADLE_HOME")"
          tar -C "$RUNNER_TEMP" -czf "$RUNNER_TEMP/local-toolchain/liliya-gradle-home.tar.gz" liliya-gradle-home
          tar -C "$ANDROID_HOME" -czf "$RUNNER_TEMP/local-toolchain/android-sdk-compile.tar.gz" \
            platforms/android-35 \
            build-tools/35.0.0 \
            platform-tools \
            licenses
          printf '%s\n' "JAVA_REAL=$(basename "$JAVA_REAL")" "GRADLE_HOME=$(basename "$GRADLE_HOME")" > "$RUNNER_TEMP/local-toolchain/MANIFEST.txt"
          du -sh "$RUNNER_TEMP/local-toolchain"/*

      - name: Upload JDK 17
        uses: actions/upload-artifact@v4
        with:
          name: liliyacore-local-jdk17-${{ github.sha }}
          path: ${{ runner.temp }}/local-toolchain/temurin-jdk17.tar.gz
          retention-days: 1
          compression-level: 0
          if-no-files-found: error

      - name: Upload Gradle runtime
        uses: actions/upload-artifact@v4
        with:
          name: liliyacore-local-gradle-${{ github.sha }}
          path: ${{ runner.temp }}/local-toolchain/gradle-9.6.1-runtime.tar.gz
          retention-days: 1
          compression-level: 0
          if-no-files-found: error

      - name: Upload project Gradle home
        uses: actions/upload-artifact@v4
        with:
          name: liliyacore-local-gradle-home-${{ github.sha }}
          path: ${{ runner.temp }}/local-toolchain/liliya-gradle-home.tar.gz
          retention-days: 1
          compression-level: 0
          if-no-files-found: error

      - name: Upload Android compile SDK
        uses: actions/upload-artifact@v4
        with:
          name: liliyacore-local-android-sdk-${{ github.sha }}
          path: ${{ runner.temp }}/local-toolchain/android-sdk-compile.tar.gz
          retention-days: 1
          compression-level: 0
          if-no-files-found: error

      - name: Upload manifest
        uses: actions/upload-artifact@v4
        with:
          name: liliyacore-local-toolchain-manifest-${{ github.sha }}
          path: ${{ runner.temp }}/local-toolchain/MANIFEST.txt
          retention-days: 1
          if-no-files-found: error
